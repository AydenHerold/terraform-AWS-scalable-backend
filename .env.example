# --- Copy this file to .env and fill in your actual values. ---
# --- IMPORTANT: Add .env to your .gitignore file! ---

# === AWS Credentials for LOCAL Terraform/AWS CLI ===
# Used when running `terraform apply` or `aws` commands locally.
# For CI/CD, these (or OIDC role) are typically set as GitHub Secrets.
AWS_ACCESS_KEY_ID="YOUR_LOCAL_AWS_ACCESS_KEY_ID"
AWS_SECRET_ACCESS_KEY="YOUR_LOCAL_AWS_SECRET_ACCESS_KEY"
AWS_REGION="us-east-1"

# === Terraform Sensitive Variables (for LOCAL `terraform apply`) ===
# Terraform automatically reads environment variables prefixed with TF_VAR_
# Alternatively, set these in a local terraform.tfvars file
TF_VAR_db_username="YOUR_RDS_DATABASE_USERNAME"
TF_VAR_db_password="YOUR_RDS_DATABASE_PASSWORD_VERY_SECRET"

# === Application Environment Variables (for LOCAL `app` execution) ===
# These would typically be used by your application code in the `app/` directory
# when running it locally. In the deployed AWS environment, these should ideally
# be fetched securely (e.g., DB creds from Secrets Manager via IAM Role).
DB_HOST="localhost_or_TF_output_rds_endpoint" # Get actual value from `terraform output rds_endpoint` after apply
DB_PORT="3306"
DB_USER="YOUR_RDS_DATABASE_USERNAME"
DB_PASSWORD="YOUR_RDS_DATABASE_PASSWORD_VERY_SECRET"
DB_NAME="YOUR_DATABASE_NAME"
APP_PORT="8080"
SESSION_SECRET="a_long_random_string_for_session_security"


# === Reference for GitHub Actions Secrets/Variables ===
# DO NOT put actual secrets here. This is just a reminder of what needs
# to be configured in your GitHub Repository Settings -> Secrets and variables -> Actions.
# --- GITHUB SECRETS ---
# If using Access Keys for CI/CD:
# GITHUB_AWS_ACCESS_KEY_ID="VALUE_STORED_IN_GITHUB_SECRETS"
# GITHUB_AWS_SECRET_ACCESS_KEY="VALUE_STORED_IN_GITHUB_SECRETS"
# Recommended if using OIDC for CI/CD:
# GITHUB_AWS_ROLE_TO_ASSUME="arn:aws:iam::123456789999:role/YourGitHubActionsRole"

# --- GITHUB VARIABLES ---
# GITHUB_AWS_REGION="us-east-1"
# GITHUB_ASSETS_BUCKET_NAME="your-bucket-name"
# GITHUB_CLOUDFRONT_DISTRIBUTION_ID="E123ABC456DEF"
